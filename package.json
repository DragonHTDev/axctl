{
  "name": "axctl-workspace",
  "version": "0.1.0",
  "description": "Monorepo for axctl (CLI), axctl-core (library) and the npm distribution",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@9.15.9",
  "scripts": {
    "axctl": "cargo run -p axctl-rs --",
    "sync-version": "node scripts/sync-version.mjs"
  }
}
